pipeline {
    agent {
      maven 'Maven 3.6.2' 
      jdk 'jdk8'
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
    }
}
